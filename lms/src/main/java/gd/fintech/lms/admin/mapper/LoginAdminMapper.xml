<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gd.fintech.lms.admin.mapper.LoginAdminMapper">
	<select id="selectAdminAccount" parameterType="gd.fintech.lms.vo.Account" resultType="java.lang.String">
		SELECT account_id
		From account
		WHERE account_id = #{accountId}
		AND account_pw = #{accountPw}
		AND account_state="활성화"
		AND account_level="1"
	</select>
</mapper>